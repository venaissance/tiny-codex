# tiny-codex 整体架构设计

轻量 Agent IDE — 代码开发 / 技术写作 / 文件预览

## 决策汇总

| 维度 | 决策 |
|------|------|
| 项目名 | **tiny-codex** |
| 技术栈 | Node.js + React + Electron |
| 模型后端 | OpenAI 兼容 + Anthropic 原生（双 Provider） |
| 执行模式 | Local + Worktree |
| 进程架构 | 单进程多线程 + 按需 spawn（方案 D） |
| 工具集 | 标准 8 工具 + **Skills 可注册自定义工具**（如 image_gen） |
| 上下文管理 | 本地持久化（SQLite）+ 自动 Compaction |
| 代码编辑器 | **Monaco Editor**（语法高亮 / 编辑 / Diff 视图） |
| 预览系统 | 分屏混合（Markdown / HTML / CSV·JSON / React Dev / **图片** / **PDF**） |
| MVP 功能 | 线程管理 / 模型选择 / Open·Commit / Skills（**含输入框 Skill 标签**） / 快捷建议 |

## 目标场景

| 场景 | Agent 做什么 | 预览需求 |
|------|------------|---------|
| 前端开发 | 读写 React 代码、跑 dev server | 实时预览运行中的页面 |
| 技术写作 | 生成/编辑 Markdown 文章 | Markdown 渲染预览（支持本地图片） |
| 数据/文档 | 处理 CSV/JSON/HTML 文件 | 结构化预览 |
| 图片生成 | 通过 Skill 调用图片生成 API | 图片预览（单张/画廊） |
| 文档阅读 | 打开和浏览 PDF 文件 | PDF 渲染预览 |

## 进程架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Electron App (tiny-codex)                        │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Renderer Process (React)                      │   │
│  │                                                                  │   │
│  │  ┌─────────┐  ┌───────────────────────┐  ┌──────────────────┐   │   │
│  │  │Sidebar  │  │    Chat Panel          │  │  Preview Panel   │   │   │
│  │  │         │  │                        │  │                  │   │   │
│  │  │• Threads│  │  Message History       │  │  ┌────────────┐ │   │   │
│  │  │• Skills │  │  Tool Outputs          │  │  │ Monaco     │ │   │   │
│  │  │         │  │  Code Diffs (Monaco)   │  │  │ (编辑/Diff)│ │   │   │
│  │  │         │  │                        │  │  │ Markdown   │ │   │   │
│  │  │         │  │  Loading States        │  │  │ HTML       │ │   │   │
│  │  │         │  │                        │  │  │ CSV/JSON   │ │   │   │
│  │  │         │  │                        │  │  │ React Dev  │ │   │   │
│  │  │         │  │                        │  │  │ Image      │ │   │   │
│  │  │         │  │                        │  │  │ PDF        │ │   │   │
│  │  │         │  │  ┌──────────────────┐  │  │  │ (webview)  │ │   │   │
│  │  │         │  │  │ Input + Actions  │  │  │  └────────────┘ │   │   │
│  │  │         │  │  │ Model Picker     │  │  │  └────────────┘ │   │   │
│  │  │         │  │  │ Local/Worktree   │  │  │                  │   │   │
│  │  └─────────┘  └──────────────────────┘  └──────────────────┘   │   │
│  └──────────────────────────┬───────────────────────────────────────┘   │
│                             │ IPC (contextBridge)                       │
│  ┌──────────────────────────▼───────────────────────────────────────┐   │
│  │                    Main Process (Node.js)                        │   │
│  │                                                                  │   │
│  │  ┌────────────────────── Shared ──────────────────────────────┐  │   │
│  │  │  API Client Pool    │  SQLite DB    │  Model Registry      │  │   │
│  │  └────────────────────────────────────────────────────────────┘  │   │
│  │                                                                  │   │
│  │  ┌─────────────── Thread Manager ────────────────────────────┐  │   │
│  │  │                                                            │  │   │
│  │  │  ┌─ Thread 1 ──┐  ┌─ Thread 2 ──┐  ┌─ Thread N ──┐      │  │   │
│  │  │  │ Agent Loop   │  │ Agent Loop   │  │ Agent Loop   │      │  │   │
│  │  │  │ Messages[]   │  │ Messages[]   │  │ Messages[]   │      │  │   │
│  │  │  │ Tools State  │  │ Tools State  │  │ Tools State  │      │  │   │
│  │  │  │ Abort Ctrl   │  │ Abort Ctrl   │  │ Abort Ctrl   │      │  │   │
│  │  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │  │   │
│  │  │         │ spawn           │ spawn           │ spawn        │  │   │
│  │  └─────────┼─────────────────┼─────────────────┼──────────────┘  │   │
│  │            ▼                 ▼                 ▼                  │   │
│  │      ┌──────────┐     ┌──────────┐      ┌──────────┐            │   │
│  │      │ bash/zsh │     │ bash/zsh │      │ bash/zsh │            │   │
│  │      │ (子进程)  │     │ (子进程)  │      │ (子进程)  │            │   │
│  │      └──────────┘     └──────────┘      └──────────┘            │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```
